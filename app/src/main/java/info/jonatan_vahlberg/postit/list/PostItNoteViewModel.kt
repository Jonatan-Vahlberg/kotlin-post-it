package info.jonatan_vahlberg.postit.list

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class PostItNoteViewModel(application: Application): AndroidViewModel(application) {
}